function getComputerChoice {
    //set 'rock' as variable (not case dependent)
    //set 'paper' as variable (not case dependent)
    //set 'scissors' as variable (not case dependent)
    //computer selects one variable
    //result = Does this need to be a different value?
}

//create interface for user choice = playerSelection

function declaration {
    //take playerSelection
    //take getComputerChoice (or alternative result variable)
    //if/else
}